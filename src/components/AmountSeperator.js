import styled from 'styled-components'

import { colors } from '../theme'

const AmountSeperator = styled.div`
  width: 100%;
  height: 1px;
  background: ${colors.white};
  margin: 1.6rem 0 1.1rem 0;
  opacity: .2;
`

export default AmountSeperator
